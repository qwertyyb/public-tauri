{
  "name": "raycast-view-basic",
  "title": "Raycast View Basic",
  "description": "Fixture for Raycast view command compatibility in Node Worker",
  "type": "module",
  "commands": [
    {
      "name": "search",
      "title": "Raycast View Search",
      "subtitle": "Validate Worker-rendered List, Detail, and ActionPanel",
      "description": "Shows a Raycast List rendered from the Node Worker.",
      "mode": "view",
      "keywords": ["raycast", "view", "worker", "search"]
    },
    {
      "name": "detail",
      "title": "Raycast View Detail",
      "subtitle": "Validate standalone Worker-rendered Detail view",
      "description": "Shows a Raycast Detail command rendered from the Node Worker.",
      "mode": "view",
      "keywords": ["raycast", "view", "worker", "detail"]
    },
    {
      "name": "form",
      "title": "Raycast View Form",
      "subtitle": "Validate Worker-rendered Form view",
      "description": "Shows a Raycast Form command rendered from the Node Worker.",
      "mode": "view",
      "keywords": ["raycast", "view", "worker", "form"]
    },
    {
      "name": "arguments",
      "title": "Raycast View With Arguments",
      "subtitle": "Validate arguments form before Worker view command",
      "description": "Shows a Raycast Detail view command receiving launch arguments.",
      "mode": "view",
      "keywords": ["raycast", "view", "worker", "arguments"],
      "arguments": [
        {
          "name": "title",
          "placeholder": "Input title",
          "type": "text",
          "required": true
        },
        {
          "name": "secret",
          "placeholder": "Input secret",
          "type": "password",
          "required": true
        },
        {
          "name": "favoriteColor",
          "type": "dropdown",
          "placeholder": "Favorite Color",
          "required": true,
          "data": [
            {
              "title": "Red",
              "value": "red"
            },
            {
              "title": "Green",
              "value": "green"
            },
            {
              "title": "Blue",
              "value": "blue"
            }
          ]
        }
      ]
    }
  ],
  "preferences": [
    {
      "name": "message",
      "title": "Message",
      "type": "textfield",
      "required": false,
      "default": "hello from worker-rendered raycast view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.0.0"
  }
}
